eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

  server:
    enable-self-preservation: true
spring:
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

